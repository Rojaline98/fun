using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;

namespace CodefirstAssignment
{
    internal class Class1
    {
        Model1 dc = new Model1();
        Repo r = new Repo();
        public void AddnewStudents()
        {
            //3 steps
            //1. create object of student class
            //try
            //{
            //    Department s = new Department() { Deptid = 1, DepartmentName = "HR", Manager = "Srinivash", DepartmentEdit =  };
            //    Employee e = new Employee() { Empid= "one", EmpName= "rojaline", Deptid= s, Salary= 60000, DOB=   };



            //    r.Add(s);
            //}
            //catch (Exception ex)
            //{
            //    var res = dc.GetValidationErrors();

            //    foreach (var item in res)
            //    {
            //        if (item.ValidationErrors.Count > 0)
            //        {
            //            foreach (var err in item.ValidationErrors)
            //            {
            //                Console.WriteLine(err.ErrorMessage);
            //            }
            //        }
            //    }
            //}

        }
    }
}

